import classNames from "classnames/bind";

export const dropDownContainer = classNames(["flex flex-col justify-end"]);
export const createButtonStyle = classNames([
  "mt-4 border-0 block bg-blue-800 hover:bg-blue-600 text-white font-bold rounded-full px-11 py-3 overflow-hidden focus:outline-none focus:border-white",
]);
export const li = classNames([
  "block",
  "px-4",
  "py-1",
  "rounded-t-lg",
  "text-blue-900",
  "cursor-pointer",
  "hover:text-purple-600",
]);
