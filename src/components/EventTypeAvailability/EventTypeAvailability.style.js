import classNames from "classnames/bind";

export const availabilityContainer = classNames(["flex flew-wrap w-full"]);
export const scheduleContainer = classNames(["form-select block w-60 rounded"]);
export const scheduleTitle = classNames(["w-1/3 text-purple-400"]);
export const weeklyHoursContainer = classNames(["w-full flex justify-center"]);
