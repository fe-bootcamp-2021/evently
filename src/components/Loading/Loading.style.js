import classNames from "classnames/bind";

export const loadingContainer = classNames([
  "w-full h-screen flex justify-center items-center",
]);
export const loading = classNames([
  "animate-spin rounded-full h-32 w-32 border-t-2 border-b-2 border-purple-700",
]);
